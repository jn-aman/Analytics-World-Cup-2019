<meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=0.5">


<style>
A.class1 {color:grey;}
A.class1:link  { color: grey;}
A.class1:visited {text-decoration: none; color: grey;}
A.class1:hover {text-decoration: none; color: white;}
A.class1:active {text-decoration: none; color: white;}

    /* style the elements with CSS */
    #pleaserotate-graphic{
        fill: #fff;
    }

    #pleaserotate-backdrop {
        color: #fff;
        background-color: #000;
    }


</style>




  


<script
  src="https://code.jquery.com/jquery-3.4.1.js"
  integrity="sha256-WpOohJOqMqqyKL9FccASB9O0KwACQJpFTUBLTYOVvVU="
  crossorigin="anonymous"></script> 









     <script type="text/javascript">
   	
   	// Change the selector if needed
var $table = $('table.scroll'),
    $bodyCells = $table.find('tbody tr:first').children(),
    colWidth;

// Adjust the width of thead cells when window resizes
$(window).resize(function() {
    // Get the tbody columns width array
    colWidth = $bodyCells.map(function() {
        return $(this).width();
    }).get();
    
    // Set the width of thead columns
    $table.find('thead tr').children().each(function(i, v) {
        $(v).width(colWidth[i]);
    });    
}).resize(); // Trigger resize handler
   </script>

<style type="text/css">
	table.scroll {
    /* width: 100%; */ /* Optional */
    /* border-collapse: collapse; */
    border-spacing: 0;
    border: 2px solid black;
}

.center {margin: auto 0px;}

table.scroll tbody,
table.scroll thead { display: block; }


thead tr  { 
    height: 30px;
    line-height: 30px;
     text-align: center; 
}


table.scroll tbody {
    height: 100px;
    overflow-y: auto;
    overflow-x: hidden;
}

tbody { 

	border-top: 2px solid black;
	 }

tbody td {
     /*width: 20%;   Optional */
    /*border-right: 1px solid black;*/
        border: 1px solid black;

    /* white-space: nowrap; */
}

tbody th{
     text-align: center; 
}

 


tbody td:last-child,{
    border-right: none;
}



</style>

<script type="text/javascript">
	// First we get the viewport height and we multiple it by 1% to get a value for a vh unit
let vh = window.innerHeight * 0.01;
// Then we set the value in the --vh custom property to the root of the document
document.documentElement.style.setProperty('--vh', `${vh}px`);

// We listen to the resize event
window.addEventListener('resize', () => {
  // We execute the same script as before
  let vh = window.innerHeight * 0.01;
  document.documentElement.style.setProperty('--vh', `${vh}px`);
});
</script>
  
     <style type="text/css">
     	
     	body {
  margin: 0;
  padding: 0;
  width: 100%;
  height: 100%;
}
#scroll {
  position: absolute;
  width: 100%;
  height: 100%;
  overflow: auto;
}
.pages {
  width: 100%;
  height: 100vh;
}
     </style>
    
    



<center>




<div id="scroll">


<div class="pages" id="one" >
<h1 class="display-2"><%= @xx.titleize %> vs Others</h1>

<%= line_chart @s ,  width: "800px", height: "400px" ,xtitle: "Matches", ytitle: "Tweets", dataset: {borderWidth: 2}, download: {filename: "top10twitterer"}%>

<div id="group" class="btn " role="group" aria-label="Basic example" >
	
				<%for i in @t.keys do %>
  		
  		<button class="btn btn-outline-primary "> <%= i.upcase  %> </button>
				<% end %>
  
  


</div>



 <center> <div id="divResults"></div></center>
    


</div>


<div class="pages" id="two" >
	<h1 class="display-2">Indian Cricketers </h1>


<%= area_chart @ind , stacked: true,  width: "800px", height: "400px" ,xtitle: "Inidan Cricketers", ytitle: "Statistics", dataset: {borderWidth: 3}, download: {filename: "top10twitterer"}%>



<div id="group1" class="btn " role="group" aria-label="Basic example">
	
				<%for i in @t1.keys do %>
  		
  		<button class="btn btn-outline-primary btn-sm center " value=<%= i  %> > <%= i.split('_').first.upcase  %> </button>
				<% end %>
  
</div>
 <center> <div id="divResults1"></div></center>

</div>

<div class="pages" id="three">
<h1 class="display-2">Pakistani Cricketers </h1>


<%= area_chart @pak , stacked: true,  width: "800px", height: "400px" ,xtitle: "Pakistan Cricketers", ytitle: "Statistics", dataset: {borderWidth: 3}, download: {filename: "top10twitterer"}%>




<div id="group2" class="btn " role="group" aria-label="Basic example">
	
				<%for i in @t2.keys do %>
  		
  		<button class="btn btn-outline-primary btn-sm " value=<%= i  %> > <%= i.split('_').first.upcase  %> </button>
				<% end %>
  
</div>
 <center> <div id="divResults2"></div></center>



</div>
<div class="pages" id="four">
	<h1 class="display-2">England Cricketers </h1>


<%= area_chart @eng , stacked: true,  width: "800px", height: "400px" ,xtitle: "England Cricketers", ytitle: "Statistics", dataset: {borderWidth: 3}, download: {filename: "top10twitterer"}%>



<div id="group3" class="btn " role="group" aria-label="Basic example">
	
				<%for i in @t3.keys do %>
  		
  		<button class="btn btn-outline-primary btn-sm " value=<%= i  %> > <%= i.split('_').first.upcase  %> </button>
				<% end %>
  
</div>
 <center> <div id="divResults3"></div></center>
</div>

<div class="pages" id="five">
	<h1 class="display-2">Afganistan Cricketers </h1>

<%= area_chart @afg , stacked: true,  width: "800px", height: "400px" ,xtitle: "Afganistan Cricketers", ytitle: "Statistics", dataset: {borderWidth: 3}, download: {filename: "top10twitterer"}%>


<div id="group4" class="btn " role="group" aria-label="Basic example">
	
				<%for i in @t4.keys do %>
  		
  		<button class="btn btn-outline-primary btn-sm " value=<%= i  %> > <%= i.split('_').first.upcase  %> </button>
				<% end %>
  
</div>
 <center> <div id="divResults4"></div></center>
</div>
<div class="pages" id="six">
	<h1 class="display-2">Australian Cricketers </h1>


<%= area_chart @aus , stacked: true,  width: "800px", height: "400px" ,xtitle: "Australian Cricketers", ytitle: "Statistics", dataset: {borderWidth: 3}, download: {filename: "top10twitterer"}%>


<div id="group5" class="btn " role="group" aria-label="Basic example">
	
				<%for i in @t5.keys do %>
  		
  		<button class="btn btn-outline-primary btn-sm " value=<%= i  %> > <%= i.split('_').first.upcase  %> </button>
				<% end %>
  
</div>
 <center> <div id="divResults5"></div></center>

</div>
<div class="pages" id="seven">
	<h1 class="display-2">Bangladeshian Cricketers </h1>

<%= area_chart @ban , stacked: true,  width: "800px", height: "400px" ,xtitle: "Bangladeshian Cricketers", ytitle: "Statistics", dataset: {borderWidth: 3}, download: {filename: "top10twitterer"}%>


<div id="group6" class="btn " role="group" aria-label="Basic example">
	
				<%for i in @t6.keys do %>
  		
  		<button class="btn btn-outline-primary btn-sm " value=<%= i  %> > <%= i.split('_').first.upcase  %> </button>
				<% end %>
  
</div>
 <center> <div id="divResults6"></div></center>
</div>
<div class="pages" id="eight">
<h1 class="display-2">New Zealand Cricketers </h1>

<%= area_chart @new , stacked: true,  width: "800px", height: "400px" ,xtitle: "New Zealand Cricketers", ytitle: "Statistics", dataset: {borderWidth: 3}, download: {filename: "top10twitterer"}%>


<div id="group7" class="btn " role="group" aria-label="Basic example">
	
				<%for i in @t7.keys do %>
  		<% if(i.split('_').first.blank?) %>
  		<button class="btn btn-outline-primary btn-sm " value=<%= i  %> > <%= i.split('_').second.upcase  %> </button>

  		<% else %>
  		<button class="btn btn-outline-primary btn-sm " value=<%= i  %> > <%= i.split('_').first.upcase  %> </button>
				
		<% end %>
		<% end %>
  
</div>
 <center> <div id="divResults7"></div></center>
</div>
<div class="pages" id="nine">
	<h1 class="display-2">South African Cricketers </h1>

<%= area_chart @sou , stacked: true,  width: "800px", height: "400px" ,xtitle: "South African Cricketers", ytitle: "Statistics", dataset: {borderWidth: 3}, download: {filename: "top10twitterer"}%>


<div id="group8" class="btn " role="group" aria-label="Basic example">
	
				<%for i in @t8.keys do %>
  		
  		<button class="btn btn-outline-primary btn-sm " value=<%= i  %> > <%= i.split('_').first.upcase  %> </button>
				<% end %>
  
</div>
 <center> <div id="divResults8"></div></center>
</div>
<div class="pages" id="ten">
	<h1 class="display-2">Sri Lankan Cricketers  </h1>

<%= area_chart @sri , stacked: true,  width: "800px", height: "400px" ,xtitle: "Sri Lankan Cricketers", ytitle: "Statistics", dataset: {borderWidth: 3}, download: {filename: "top10twitterer"}%>


<div id="group9"class="btn " role="group" aria-label="Basic example">
	
				<%for i in @t9.keys do %>
  		
  		<button class="btn btn-outline-primary btn-sm " value=<%= i  %> > <%= i.split('_').first.upcase  %> </button>
				<% end %>
  
</div>
 <center> <div id="divResults9"></div></center>
</div>
<div class="pages" id="eleven" >
	<h1 class="display-2">West Indies Cricketers</h1>

<%= area_chart @wes , stacked: true,  width: "800px", height: "400px" ,xtitle: "West Indies Cricketers", ytitle: "Statistics", dataset: {borderWidth: 3}, download: {filename: "top10twitterer"}%>

<div id="group10" class="btn " role="group" aria-label="Basic example">
	
				<%for i in @t10.keys do %>
  		
  		<button class="btn btn-outline-primary btn-sm " value=<%= i  %> > <%= i.split('_').first.upcase  %> </button>
				<% end %>
  
</div>
 <center> <div id="divResults10"></div></center>

<div class="btn1" role="group" aria-label="Basic example">
<br><br>
  <button type="button" class="btn btn-outline-secondary"><a class="class1" href="/crickets/country">Back</a></button>
  <button type="button" class="btn btn-outline-secondary"><a class="class1" href="/crickets/slider">Back to Home Page</a></button>
</div>
</div>
</center>
</div> 

<script type="text/javascript">
	// for country tweets

if($("#divResults").length != 0)
{
$("#divResults").toggle();
}
	var country;
	$('#group').on('click', 'button', function(event) {
  var country = this.textContent;



$("#divResults").toggle();



country=country.trim().toLowerCase();

console.log(country);


var availableTags={};

	 availableTags= <%= raw json_escape(@t.to_json) %>;
	

console.log(availableTags[country]);


if($("#divResults").length) {
  $("#divResults").html("");

}


if(availableTags[country].length > 0)
{




	$('#divResults').append('<table id="myTable" class="scroll"><thead ><tr><th align="center">'+ "Tweets"+'</th></tr></thead><tbody></tbody></table>' );
for(i=0;i<availableTags[country].length;i++)
$('#myTable > tbody:last-child').append('<tr><td>'+availableTags[country][i]+'<td></tr>');

}

$('#divResults').append('<br>')

});


</script>


<script type="text/javascript">
	// for india tweets

if($("#divResults1").length != 0)
{
$("#divResults1").toggle();
}
	var country;
	$('#group1').on('click', 'button', function(event) {
  var country = this.value;
$("#divResults1").toggle();
country=country.trim().toLowerCase();
console.log(country);
var availableTags={};
	 availableTags= <%= raw json_escape(@t1.to_json) %>;
if($("#divResults1").length) {
  $("#divResults1").html("");
}
if(availableTags[country].length > 0)
{	$('#divResults1').append('<table id="myTable" class="scroll"><thead ><tr><th align="center">'+ "Tweets"+'</th></tr></thead><tbody></tbody></table>' );
for(i=0;i<availableTags[country].length;i++)
$('#myTable > tbody:last-child').append('<tr><td>'+availableTags[country][i]+'<td></tr>');
}
$('#divResults1').append('<br>')
});


</script>

<script type="text/javascript">
	// for india tweets

if($("#divResults2").length != 0)
{
$("#divResults2").toggle();
}
	var country;
	$('#group2').on('click', 'button', function(event) {
  var country = this.value;
$("#divResults2").toggle();
country=country.trim().toLowerCase();
console.log(country);
var availableTags={};
	 availableTags= <%= raw json_escape(@t2.to_json) %>;
if($("#divResults2").length) {
  $("#divResults2").html("");
}
if(availableTags[country].length > 0)
{	$('#divResults2').append('<table id="myTable" class="scroll"><thead ><tr><th align="center">'+ "Tweets"+'</th></tr></thead><tbody></tbody></table>' );
for(i=0;i<availableTags[country].length;i++)
$('#myTable > tbody:last-child').append('<tr><td>'+availableTags[country][i]+'<td></tr>');
}
$('#divResults2').append('<br>')
});


</script>
<script type="text/javascript">
	// for india tweets

if($("#divResults3").length != 0)
{
$("#divResults3").toggle();
}
	var country;
	$('#group3').on('click', 'button', function(event) {
  var country = this.value;
$("#divResults3").toggle();
country=country.trim().toLowerCase();
console.log(country);
var availableTags={};
	 availableTags= <%= raw json_escape(@t3.to_json) %>;
if($("#divResults3").length) {
  $("#divResults3").html("");
}
if(availableTags[country].length > 0)
{	$('#divResults3').append('<table id="myTable" class="scroll"><thead ><tr><th align="center">'+ "Tweets"+'</th></tr></thead><tbody></tbody></table>' );
for(i=0;i<availableTags[country].length;i++)
$('#myTable > tbody:last-child').append('<tr><td>'+availableTags[country][i]+'<td></tr>');
}
$('#divResults3').append('<br>')
});


</script>
<script type="text/javascript">
	// for india tweets

if($("#divResults4").length != 0)
{
$("#divResults4").toggle();
}
	var country;
	$('#group4').on('click', 'button', function(event) {
  var country = this.value;
$("#divResults4").toggle();
country=country.trim().toLowerCase();
console.log(country);
var availableTags={};
	 availableTags= <%= raw json_escape(@t4.to_json) %>;
if($("#divResults4").length) {
  $("#divResults4").html("");
}
if(availableTags[country].length > 0)
{	$('#divResults4').append('<table id="myTable" class="scroll"><thead ><tr><th align="center">'+ "Tweets"+'</th></tr></thead><tbody></tbody></table>' );
for(i=0;i<availableTags[country].length;i++)
$('#myTable > tbody:last-child').append('<tr><td>'+availableTags[country][i]+'<td></tr>');
}
$('#divResults4').append('<br>')
});


</script>
<script type="text/javascript">
	// for india tweets

if($("#divResults5").length != 0)
{
$("#divResults5").toggle();
}
	var country;
	$('#group5').on('click', 'button', function(event) {
  var country = this.value;
$("#divResults5").toggle();
country=country.trim().toLowerCase();
console.log(country);
var availableTags={};
	 availableTags= <%= raw json_escape(@t5.to_json) %>;
if($("#divResults5").length) {
  $("#divResults5").html("");
}
if(availableTags[country].length > 0)
{	$('#divResults5').append('<table id="myTable" class="scroll"><thead ><tr><th align="center">'+ "Tweets"+'</th></tr></thead><tbody></tbody></table>' );
for(i=0;i<availableTags[country].length;i++)
$('#myTable > tbody:last-child').append('<tr><td>'+availableTags[country][i]+'<td></tr>');
}
$('#divResults5').append('<br>')
});


</script>
<script type="text/javascript">
	// for india tweets

if($("#divResults6").length != 0)
{
$("#divResults6").toggle();
}
	var country;
	$('#group6').on('click', 'button', function(event) {
  var country = this.value;
$("#divResults6").toggle();
country=country.trim().toLowerCase();
console.log(country);
var availableTags={};
	 availableTags= <%= raw json_escape(@t6.to_json) %>;
if($("#divResults6").length) {
  $("#divResults6").html("");
}
if(availableTags[country].length > 0)
{	$('#divResults6').append('<table id="myTable" class="scroll"><thead ><tr><th align="center">'+ "Tweets"+'</th></tr></thead><tbody></tbody></table>' );
for(i=0;i<availableTags[country].length;i++)
$('#myTable > tbody:last-child').append('<tr><td>'+availableTags[country][i]+'<td></tr>');
}
$('#divResults6').append('<br>')
});


</script>
<script type="text/javascript">
	// for india tweets

if($("#divResults7").length != 0)
{
$("#divResults7").toggle();
}
	var country;
	$('#group7').on('click', 'button', function(event) {
  var country = this.value;
$("#divResults7").toggle();
country=country.trim().toLowerCase();
console.log(country);
var availableTags={};
	 availableTags= <%= raw json_escape(@t7.to_json) %>;
if($("#divResults7").length) {
  $("#divResults7").html("");
}
if(availableTags[country].length > 0)
{	$('#divResults7').append('<table id="myTable" class="scroll"><thead ><tr><th align="center">'+ "Tweets"+'</th></tr></thead><tbody></tbody></table>' );
for(i=0;i<availableTags[country].length;i++)
$('#myTable > tbody:last-child').append('<tr><td>'+availableTags[country][i]+'<td></tr>');
}
$('#divResults7').append('<br>')
});


</script>
<script type="text/javascript">
	// for india tweets

if($("#divResults8").length != 0)
{
$("#divResults8").toggle();
}
	var country;
	$('#group8').on('click', 'button', function(event) {
  var country = this.value;
$("#divResults8").toggle();
country=country.trim().toLowerCase();
console.log(country);
var availableTags={};
	 availableTags= <%= raw json_escape(@t8.to_json) %>;
if($("#divResults8").length) {
  $("#divResults8").html("");
}
if(availableTags[country].length > 0)
{	$('#divResults8').append('<table id="myTable" class="scroll"><thead ><tr><th align="center">'+ "Tweets"+'</th></tr></thead><tbody></tbody></table>' );
for(i=0;i<availableTags[country].length;i++)
$('#myTable > tbody:last-child').append('<tr><td>'+availableTags[country][i]+'<td></tr>');
}
$('#divResults8').append('<br>')
});


</script>
<script type="text/javascript">
	// for india tweets

if($("#divResults9").length != 0)
{
$("#divResults9").toggle();
}
	var country;
	$('#group9').on('click', 'button', function(event) {
  var country = this.value;
$("#divResults9").toggle();
country=country.trim().toLowerCase();
console.log(country);
var availableTags={};
	 availableTags= <%= raw json_escape(@t9.to_json) %>;
if($("#divResults9").length) {
  $("#divResults9").html("");
}
if(availableTags[country].length > 0)
{	$('#divResults9').append('<table id="myTable" class="scroll"><thead ><tr><th align="center">'+ "Tweets"+'</th></tr></thead><tbody></tbody></table>' );
for(i=0;i<availableTags[country].length;i++)
$('#myTable > tbody:last-child').append('<tr><td>'+availableTags[country][i]+'<td></tr>');
}
$('#divResults9').append('<br>')
});


</script>
<script type="text/javascript">
	// for india tweets

if($("#divResults10").length != 0)
{
$("#divResults10").toggle();
}
	var country;
	$('#group10').on('click', 'button', function(event) {
  var country = this.value;
$("#divResults10").toggle();
country=country.trim().toLowerCase();
console.log(country);
var availableTags={};
	 availableTags= <%= raw json_escape(@t10.to_json) %>;
if($("#divResults10").length) {
  $("#divResults10").html("");
}
if(availableTags[country].length > 0)
{	$('#divResults10').append('<table id="myTable" class="scroll"><thead ><tr><th align="center">'+ "Tweets"+'</th></tr></thead><tbody></tbody></table>' );
for(i=0;i<availableTags[country].length;i++)
$('#myTable > tbody:last-child').append('<tr><td>'+availableTags[country][i]+'<td></tr>');
}
$('#divResults10').append('<br>')
});


</script>
<script>
  var portrait = window.innerWidth < window.innerHeight;

  var PleaseRotateOptions = {
      onlyMobile: false,
      forcePortrait: !portrait
  }
 </script>

 <script type="text/javascript">
	

function ScrollHandler(pageId) {
  var page = document.getElementById(pageId);
  var pageStart = page.offsetTop;
  var pageJump = false;
  var viewStart;
  var duration = 1000;
  var scrolled = document.getElementById("scroll");

  function scrollToPage() {
    pageJump = true;

    // Calculate how far to scroll
    var startLocation = viewStart;
    var endLocation = pageStart;
    var distance = endLocation - startLocation;

    var runAnimation;

    // Set the animation variables to 0/undefined.
    var timeLapsed = 0;
    var percentage, position;

    var easing = function(progress) {
      return progress < 0.5
        ? 4 * progress * progress * progress
        : (progress - 1) * (2 * progress - 2) * (2 * progress - 2) + 1; // acceleration until halfway, then deceleration
    };

    function stopAnimationIfRequired(pos) {
      if (pos == endLocation) {
        cancelAnimationFrame(runAnimation);
        setTimeout(function() {
          pageJump = false;
        }, 500);
      }
    }

    var animate = function() {
      timeLapsed += 16;
      percentage = timeLapsed / duration;
      if (percentage > 1) {
        percentage = 1;
        position = endLocation;
      } else {
        position = startLocation + distance * easing(percentage);
      }
      scrolled.scrollTop = position;
      runAnimation = requestAnimationFrame(animate);
      stopAnimationIfRequired(position);
      console.log("position=" + scrolled.scrollTop + "(" + percentage + ")");
    };
    // Loop the animation function
    runAnimation = requestAnimationFrame(animate);
  }


  window.addEventListener("wheel", function(event) {
    viewStart = scrolled.scrollTop;
    if (!pageJump) {
      var pageHeight = page.scrollHeight;
      var pageStopPortion = pageHeight / 2;
      var viewHeight = window.innerHeight;

      var viewEnd = viewStart + viewHeight;
      var pageStartPart = viewEnd - pageStart;
      var pageEndPart = pageStart + pageHeight - viewStart;

      var canJumpDown = pageStartPart >= 0;
      var stopJumpDown = pageStartPart > pageStopPortion;

      var canJumpUp = pageEndPart >= 0;
      var stopJumpUp = pageEndPart > pageStopPortion;

      var scrollingForward = event.deltaY > 0;
      if (
        (scrollingForward && canJumpDown && !stopJumpDown) ||
        (!scrollingForward && canJumpUp && !stopJumpUp)
      ) {
        event.preventDefault();
        scrollToPage();
      }
      false; //
    } else {
      event.preventDefault();
    }
  });
}
new ScrollHandler("one");
new ScrollHandler("two");
new ScrollHandler("three");
new ScrollHandler("four");
new ScrollHandler("five");
new ScrollHandler("six");
new ScrollHandler("seven");
new ScrollHandler("eight");
new ScrollHandler("nine");
new ScrollHandler("ten");
new ScrollHandler("eleven");

</script>
